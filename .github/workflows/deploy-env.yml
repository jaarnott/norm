name: Deploy Environment

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      image_tag:
        required: true
        type: string

jobs:
  deploy:
    name: Deploy to ${{ inputs.environment }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    steps:
      - uses: actions/checkout@v4

      # TODO (Phase 3): Authenticate with GCP
      # - uses: google-github-actions/auth@v2
      #   with:
      #     credentials_json: ${{ secrets.GCP_SA_KEY }}
      # - uses: google-github-actions/setup-gcloud@v2

      # TODO (Phase 3): Run database migrations via Cloud Run Job
      # - name: Run migrations
      #   run: |
      #     gcloud run jobs execute norm-migrate \
      #       --region ${{ vars.GCP_REGION }} \
      #       --wait \
      #       --set-env-vars DATABASE_URL=${{ secrets.DATABASE_URL }}

      # TODO (Phase 3): Deploy API to Cloud Run
      # - name: Deploy API
      #   run: |
      #     gcloud run deploy norm-api \
      #       --image ${{ vars.REGISTRY }}/norm-api:${{ inputs.image_tag }} \
      #       --region ${{ vars.GCP_REGION }} \
      #       --platform managed

      # TODO (Phase 3): Deploy Web to Cloud Run
      # - name: Deploy Web
      #   run: |
      #     gcloud run deploy norm-web \
      #       --image ${{ vars.REGISTRY }}/norm-web:${{ inputs.image_tag }} \
      #       --region ${{ vars.GCP_REGION }} \
      #       --platform managed

      - name: Smoke test
        run: |
          echo "TODO: Smoke test ${{ inputs.environment }} environment"
          # curl -f https://${{ inputs.environment }}.norm.example.com/health || exit 1

      # Report deploy status back to Norm API
      # TODO (Phase 3): Uncomment when deploy webhook is live
      # - name: Report deploy status
      #   if: always()
      #   run: |
      #     curl -X POST "https://${{ inputs.environment }}.norm.example.com/api/admin/deploy-webhook" \
      #       -H "Content-Type: application/json" \
      #       -d '{
      #         "environment": "${{ inputs.environment }}",
      #         "image_tag": "${{ inputs.image_tag }}",
      #         "git_sha": "${{ github.sha }}",
      #         "status": "${{ job.status }}",
      #         "logs_url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
      #       }'
