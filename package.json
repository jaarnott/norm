{
  "name": "norm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @norm/web dev",
    "dev:api": "cd apps/api && .venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "pnpm --filter @norm/web build",
    "lint": "pnpm --filter @norm/web lint"
  }
}
